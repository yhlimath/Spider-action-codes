--- python_implementation/dtl_transfer_matrix.py
+++ python_implementation/dtl_transfer_matrix.py
@@ -107,10 +107,4 @@
         print("  [" + ", ".join(row_str) + "]")

 if __name__ == "__main__":
-    import sys
     L = 3
     j = 1
     if len(sys.argv) > 2:
         L = int(sys.argv[1])
         j = int(sys.argv[2])

-    T, states = construct_dtl_transfer_matrix(L, j)
-    print(f"Transfer Matrix for L={L}, j={j} (dim={len(states)}):")
-    for i, row in enumerate(T):
-        print(f"Row {i} (|{states[i]}>): {row}")
-
-if __name__ == "__main__":
-    if len(sys.argv) > 2:
-        analyze_transfer_matrix(L, j)
+    analyze_transfer_matrix(L, j)
